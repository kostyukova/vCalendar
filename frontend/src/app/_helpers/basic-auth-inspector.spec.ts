import { BasicAuthInspector } from './basic-auth-inspector';

describe('BasicAuthInspector', () => {
  it('should create an instance', () => {
    expect(new BasicAuthInspector()).toBeTruthy();
  });
});
